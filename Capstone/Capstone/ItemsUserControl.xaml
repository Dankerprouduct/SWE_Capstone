<UserControl x:Class="Capstone.ItemsUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Capstone"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="#303030">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Saved Products" FontSize="24" Foreground="White" Margin="10,5,0,0"/>
            <Separator VerticalAlignment="Bottom" Opacity=".5"/>

            <ScrollViewer Grid.Row="1" MaxHeight="300" Background="#252525">
                <ListBox x:Name="SavedProductList" ScrollViewer.CanContentScroll="True"
                          HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="500"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0"
                                        HorizontalAlignment="Left"
                                        Tag="{Binding}" Click="RemoveItem"
                                        Style="{StaticResource MaterialDesignFloatingActionMiniLightButton}"
                                        ToolTip="Save" Background="Transparent" BorderBrush="Transparent">
                                    <materialDesign:PackIcon
                                        Background="Transparent"
                                        Foreground="White"
                                        Kind="Trash"  Width="15" Height="15"/>
                                </Button>


                                <TextBlock Grid.Column="1" VerticalAlignment="Center" 
                                           TextTrimming="WordEllipsis"
                                           ToolTip="{Binding ProductName}"
                                           Text="{Binding ProductName}"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </ScrollViewer>

            <Separator Grid.Row="1" VerticalAlignment="Bottom"/>
        </Grid>

        <Grid Grid.Row="1">
            <lvc:CartesianChart x:Name="PriceChart" Series="{Binding Series}" />
        </Grid>


    </Grid>
</UserControl>
